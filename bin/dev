#!/usr/bin/env bash

rails log:clear
bundle install

if ! command -v overmind &> /dev/null
then
  echo "Installing overmind..."
  brew install overmind
else
  if [ -e "./.overmind.sock" ]
  then
    rm -f ./.overmind.sock
  fi
fi

overmind start
