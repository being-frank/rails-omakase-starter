---
priority: 1
---
<h2>Background</h2>

<%#= render_design_tokens(
  title:    'Variants',
  set:      :semantic,
  category: :color,
  property: :bg,
  display:  :variants
) %>

<%#= render_design_tokens(
  title:    'Interaction States',
  set:      :semantic,
  category: :color,
  property: :bg,
  display:  :states
) %>

<%= render_design_tokens(
  title:    'Tokens',
  set:      :semantic,
  category: :color,
  property: :surface,
  display:  :list
) %>
