---
priority: 2
---
<h2>Border</h2>

<%= render_design_tokens(
  title:    'Variants',
  set:      :semantic,
  category: :color,
  property: :border,
  variant:  :surface,
  display:  :variants
) %>

<%= render_design_tokens(
  title:    'Interaction States',
  set:      :semantic,
  category: :color,
  property: :border,
  variant:  :surface,
  display:  :states
) %>

<%= render_design_tokens(
  title:    'Tokens',
  set:      :semantic,
  category: :color,
  property: :border,
  variant:  :surface,
  group_by: :scale,
  display:  :list
) %>
