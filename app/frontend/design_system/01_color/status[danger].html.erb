---
priority: 4
---
<h2>Danger</h2>

<%= render_design_tokens(
  title:    'Background',
  set:      :semantic,
  category: :color,
  property: :bg,
  role:     :status,
  variant:  :danger,
  display:  :states
) %>

<%= render_design_tokens(
  title:    'Border',
  set:      :semantic,
  category: :color,
  property: :border,
  role:     :status,
  variant:  :danger,
  display:  :variants
) %>

<%= render_design_tokens(
  title:    'Text',
  set:      :semantic,
  category: :color,
  property: :text,
  role:     :status,
  variant:  :danger,
  display:  :text
) %>
