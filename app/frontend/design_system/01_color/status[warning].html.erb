---
priority: 3
---
<h2>Background</h2>

<%= render_design_tokens(
  title:    'Interaction States',
  set:      :semantic,
  category: :color,
  property: :bg,
  role:     :warning,
  display:  :states
) %>

<h2>Border</h2>

<%= render_design_tokens(
  title:    'Interaction States',
  set:      :semantic,
  category: :color,
  property: :border,
  role:     :warning,
  display:  :states
) %>
