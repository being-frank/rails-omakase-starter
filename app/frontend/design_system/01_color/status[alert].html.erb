---
priority: 4
---
<h2>Alert</h2>

<%= render_design_tokens(
  title:    'Background',
  set:      :semantic,
  category: :color,
  property: :bg,
  role:     :status,
  variant:  :alert,
  display:  :states
) %>

<%= render_design_tokens(
  title:    'Border',
  set:      :semantic,
  category: :color,
  property: :border,
  role:     :status,
  variant:  :alert,
  display:  :states
) %>

<%= render_design_tokens(
  title:    'Text',
  set:      :semantic,
  category: :color,
  property: :text,
  role:     :status,
  variant:  :alert,
  display:  :text
) %>
