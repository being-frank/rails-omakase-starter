---
priority: 1
---
<h2>Success</h2>

<%#= render_design_tokens(
  title:    'Variants',
  set:      :semantic,
  category: :color,
  property: :bg,
  role:     :success,
  display:  :variants
) %>

<%= render_design_tokens(
  title:    'Interaction States',
  set:      :semantic,
  category: :color,
  property: :bg,
  role:     :success,
  display:  :states
) %>

<%= render_design_tokens(
  title:    'Tokens',
  set:      :semantic,
  category: :color,
  property: :bg,
  role:     :success,
  display:  :list
) %>
