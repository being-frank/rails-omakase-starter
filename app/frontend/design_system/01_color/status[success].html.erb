---
priority: 1
---
<h2>Success</h2>

<%= render_design_tokens(
  title:    'Background',
  set:      :semantic,
  category: :color,
  property: :bg,
  role:     :status,
  variant:  :success,
  display:  :states
) %>

<%= render_design_tokens(
  title:    'Border',
  set:      :semantic,
  category: :color,
  property: :border,
  role:     :status,
  variant:  :success,
  display:  :states
) %>

<%= render_design_tokens(
  title:    'Text',
  set:      :semantic,
  category: :color,
  property: :text,
  role:     :status,
  variant:  :success,
  display:  :text
) %>
